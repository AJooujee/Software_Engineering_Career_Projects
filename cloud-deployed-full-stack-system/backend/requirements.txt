# Web framework used to build the backend REST API
fastapi==0.141.1

# ASGI server used to run the FastAPI application
uvicorn[standard]==0.52.4

# Environment-based application configuration
pydantic-settings==2.15.0

# Object-relational mapper used for database access
SQLAlchemy==2.0.52

# Database migration management
alembic==1.19.1

# PostgreSQL database driver with Windows-compatible binaries
psycopg[binary]==3.3.5

# Testing framework used for backend automated tests
pytest==9.1.1

# HTTP client used internally by FastAPI and Starlette TestClient
httpx2==2.12.0
# Modern password hashing with Argon2 support
pwdlib[argon2]==0.3.1

# JSON Web Token creation and validation
PyJWT==2.13.0

# OAuth2 password-form parsing required by FastAPI
python-multipart==0.0.32

# Email validation used by Pydantic user schemas
email-validator==2.3.0
